import logo from '../../assets/logo';

const Logo = () => {
    return (
        <div>
            <img src={ logo } className='logo' alt='Logo de la marca' />
        </div>
    )
}

export default Logo
